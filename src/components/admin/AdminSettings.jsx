import React from 'react';

const placeholderStyle = {
  padding: '20px',
  border: '2px dashed #ccc',
  borderRadius: '8px',
  backgroundColor: '#f9f9f9',
};

const AdminSettings = () => {
  return (
    <div style={placeholderStyle}>
      <h3>Admin Settings</h3>
      <p>This is where you would adjust application-wide settings.</p>
      <p>Toggles, configuration inputs, and other controls would go here.</p>
    </div>
  );
};

export default AdminSettings;
